SELECT * FROM users; # Select all columns from the users table

SELECT name FROM users; # Select only the name column from the users table

SELECT user_id, name FROM users; # Select user_id and name columns from the users table

SELECT DISTINCT name FROM users WHERE age = 27; # Select unique names from users where age is 27

